import React from "react";

const Dashboard = () => {
  return (
    <div
      style={{
        background: "#f2f3f7",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
      }}
    >
      Welcome to Student's Dashboard. You are logged In
    </div>
  );
};

export default Dashboard;
